name: Release

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'package.json'

jobs:
  test:
    uses: ./.github/workflows/test.yml

  release:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: |
          npm run build
          npm run bundle
          npm run build:cli

      - name: Get version
        id: version
        run: echo "VERSION=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Check if tag exists
        id: check_tag
        run: |
          if git rev-parse "v${{ steps.version.outputs.VERSION }}" >/dev/null 2>&1; then
            echo "Tag v${{ steps.version.outputs.VERSION }} already exists"
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      # Tag + release the GitHub Action FIRST so a flaky/expired npm token can
      # never block consumers pinned to @v1 / @vX.Y.Z. NPM publish is decoupled below.
      - name: Create Release Tag
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          gh release create "v${{ steps.version.outputs.VERSION }}" --title "v${{ steps.version.outputs.VERSION }}" --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Major Tag
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          git tag -f v1
          git push -f origin v1

      # Non-blocking: the CLI npm package is independent of the Action. If this
      # fails (e.g. expired NPM_TOKEN), the Action release above still succeeds.
      - name: Publish to NPM
        if: steps.check_tag.outputs.exists == 'false'
        continue-on-error: true
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
